{
  "name": "@hastebin/file-data-store",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "eslint . --ext js,ts",
    "lint:fix": "eslint . --ext js,jsx,ts,tsx  --fix",
    "lint:ci": "yarn lint --quiet",
    "types:check": "tsc --noEmit --pretty",
    "test:unit": "jest --colors --config config/jest.config.js",
    "test:unit:coverage": "yarn test:unit --ci --silent --maxWorkers 2 --collectCoverage true",
    "dev": "tsc -w",
    "build": "tsc"
  },
  "dependencies": {
    "pg": "^8.7.3"
  },
  "devDependencies": {
    "@types/pg": "^8.6.5",
    "eslint-config": "*",
    "jest": "*",
    "@hastebin/data-store-helper": "*",
    "tsconfig": "*"
  }
}
